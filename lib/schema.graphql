type user {
  _id: ID!
  name: String!
  phone: String!
  country: String!
  state: String!
  city: String!
}

type Query {
  "Returns all users"
  getUsers: [user]
  "Returns one user"
  getUser(id: ID!): user
}